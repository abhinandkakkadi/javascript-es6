Both every and some culminate every values that are available in an array and give
out a final answer rather than giving a single value inside the array or an array of response 

// Real world example

