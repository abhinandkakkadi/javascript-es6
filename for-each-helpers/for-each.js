var colors = ["red", "green", "blue"];

// the anonymous function that is present inside this is called iterator function
colors.forEach(function (color) {
  console.log(color);
});
// in simple turns the iterator function takes in every element of the array and do some operation that is specified inside that function body
